# Iteration-004 派单：管理看板研发线-工程监理线可视化

- 下发时间（本地）：2026-02-15 21:12:00 CST
- 负责人：管理看板研发线-Codex
- 目标：在管理面板完整展示“工程监理线”及其任务详情，支持管理层直接查看监理审计职责与最新状态

## 需求范围

1. 在“工作线总览”中新增并展示 `工程监理线`。
2. 在每条线“任务详情弹窗”中，支持展示工程监理线提示词与任务包。
3. 明确展示工程监理线边界：仅输出项目级监理审计报告，不修改任何项目工作输出。
4. 展示字段统一读取最新落盘文件，使用本地时间字段。

## 数据源（必须）

- `output/dashboard/worklines_overview.json`
- `output/dashboard/workline_dispatch_prompts_latest.json`
- `output/dashboard/dashboard_manifest.json`

## 交付标准（必须）

1. 工作线列表可见 `工程监理线`（含负责人、状态、进度、ETA）。
2. 点击任务详情可见：
   - 任务包列表
   - 监理提示词
   - 任务下发时间（本地）
3. 页面不展示 UTC 下发时间，仅展示本地时间。
4. 缺字段兜底显示 `-`，不报错，不阻塞刷新。
5. 提供变更说明、截图、自测记录。

## 验收检查清单

- [ ] 工程监理线在总览中可见
- [ ] 工程监理线任务详情可见
- [ ] 仅本地下发时间
- [ ] 缺字段兜底
- [ ] 变更说明+截图+自测
