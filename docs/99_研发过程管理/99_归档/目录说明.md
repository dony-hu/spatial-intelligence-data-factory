# 归档目录说明

> 文档状态：当前有效
> 角色：`docs/99_研发过程管理/99_归档/` 使用说明

## 1. 目的

本目录保存已经退出当前研发主线、但仍需要保留追溯价值的过程文档。

## 2. 当前归档规则

1. `截止2026-03-05/`：保存 `2026-03-05` 及之前的项目管理与研发主题过程文档。
2. 被正式章节吸收的过程材料，不再保留并列现行版本，只保留归档版本。
3. 新的历史过程文档继续按时间切分归档目录，而不是回流到根目录。

## 3. 使用边界

1. 归档文档只能作为追溯和证据输入，不再作为当前实施基线。
2. 当前有效的过程规则、工作流状态、冲刺计划和规格融合入口都在 `docs/99_研发过程管理/` 根下。
