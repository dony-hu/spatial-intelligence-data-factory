# 启动检查清单

## 1. 目的

本清单用于判断“多Agent多Worktree并行开发”主题是否已经完成本地可执行的启动动作，并显式区分已完成项与外部 blocker。

## 2. 启动结论

- 结论：`CONDITIONAL PASS`
- 说明：本地可完成的启动动作已完成；正式进入多 Lane 开发前，仍需补齐 Linear Epic / Story 建单。

## 3. 检查项

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| 主题目录已具备 `主题说明 / 设计说明 / 冲刺计划 / 故事` 最小组合 | `PASS` | 当前主题目录已齐套。 |
| Story backlog 已覆盖 Lane、Worktree、契约、集成、映射五类核心问题 | `PASS` | 已存在 `PAR-S1` 到 `PAR-S5` 五个故事。 |
| Ring1 正式规范已落盘 | `PASS` | `docs/10_研发与工程规范/多Agent与多Worktree并行开发规范.md` 已存在。 |
| 规划位与 6 条执行 Lane 的 branch / Worktree 已建立 | `PASS` | 本地已核对 `git worktree list`，共 `7` 个相关 Worktree。 |
| 当前轮已明确单人启动 owner | `PASS` | 当前 Codex 仅承担“规划 / 集成编排位”启动 owner。 |
| 红区与共享契约策略已有正式约束 | `PASS` | `AGENTS.md`、文档分层索引和正式规范均已覆盖。 |
| BMAD 方法来源已明确 | `PASS` | 方法仓不可用并采用项目内 `_bmad` 回退配置。 |
| Linear Epic / Story 已完成建单并绑定 owner | `BLOCKED` | 当前本地无 Linear 建单能力，不能伪造 Issue / Story 编号。 |

## 4. 阻塞项

1. 需要在 Linear 中创建本 Epic 及 `PAR-S1` 到 `PAR-S5` 对应 Story。
2. 建单完成后，需把 Story / branch / Worktree / owner / PR 的映射回填到本主题文档或对应执行记录。

## 5. 启动后第一批动作

1. 由规划 / 集成编排位确认 `48h contract sprint` 的唯一契约 PR 窗口。
2. 由 `lane-03` 先输出共享契约冻结清单。
3. 由 `lane-06` 输出 baseline、已知失败清单和统一验证入口。
4. Linear 建单完成后，再允许各执行 Lane 进入真实实现。
