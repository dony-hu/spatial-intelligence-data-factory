# Story：TEST-E2E-S4 blocked归因分型与环境矩阵收敛

## 状态

待开始

## 目标

让 E2E 中的 `blocked` 结果从笼统状态升级为可治理、可豁免、可恢复的结构化结论。

## 验收标准

1. 至少区分外部 LLM、能力缺口、关键 key 缺失、基础设施不可用四类责任域。
2. 环境矩阵明确哪些依赖必须真实、哪些不可用时应 `blocked`。
3. 每条 `blocked` 用例都给出恢复动作。

## 交付物

1. `docs/09_测试与验收/测试方案总览.md`
2. `docs/09_测试与验收/全链路测试设计.md`
